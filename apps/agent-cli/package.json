{
  "name": "@deskcrew/agent-cli",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "deskcrew-agent": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun --hot ./src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@deskcrew/agent-core": "workspace:*",
    "@opentui/core": "^0.2.10",
    "@opentui/solid": "^0.2.10",
    "solid-js": "^1.9.12"
  }
}
